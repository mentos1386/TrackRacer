import Canvas from './modules/canvas/Canvas';
import './style.css';

export default class Main {
  constructor() {
    // Create canvas
    const canvas = new Canvas('gameCanvas');

    // Initiate game loop
  }
}

new Main();
